apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "visitorsapp.fullname" . }}-test-connection"
  labels:
    {{- include "visitorsapp.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "visitorsapp.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
